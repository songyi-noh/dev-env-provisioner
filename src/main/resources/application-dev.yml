spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_env_provisioner
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true